/** @type {import('next').NextConfig} */
const nextConfig = {
  //   output: "export", When deploying uncommit and run npm run build
  // typescript: {ignoreBuildErrors: true,}
};

export default nextConfig;
